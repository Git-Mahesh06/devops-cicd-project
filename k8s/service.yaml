apiVersion: v1
kind: Service
metadata:
  name: prt-service
spec:
  type: NodePort
  selector:
    app: prt
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007apiVersion: v1
kind: Service
metadata:
  name: prt-service
spec:
  type: NodePort
  selector:
    app: prt
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
